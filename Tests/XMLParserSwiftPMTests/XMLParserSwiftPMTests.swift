import XCTest
@testable import XMLParserSwiftPM

final class XMLParserSwiftPMTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(XMLParserSwiftPM().text, "Hello, World!")
    }
}
